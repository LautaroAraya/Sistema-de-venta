services:
  - type: web
    name: sistema-ventas-panel-tecnico
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api_tecnico_remoto:app --bind 0.0.0.0:$PORT
    autoDeploy: true
